<?php

	/**
	 * Generated by Getz Framework
	 *
	 * @author Mario Sakamoto <mskamot@gmail.com>
	 * @see https://wtag.com.br/getz
	 */
	 
	use lib\getz;
	use src\logic;	 
	use src\model;	 
	
	require_once($_DOCUMENT_ROOT . "/lib/getz/Activator.php");
	
	/*
	 * Filters
	 */
	$where = "";
	
	if ($search != "")
		$where = "tipos_noticias.tipo_noticia LIKE \"%" . $search . "%\"";	
		
	if ($code != "")
		$where = "tipos_noticias.id = " . $code;
	
	if (isset($_GET["friendly"]))
		$where = "tipos_noticias.tipo_noticia = \"" . removeLine($_GET["friendly"]) . "\"";	
		
	$limit = "";	
	
	if ($order != "") {
		$o = explode("<gz>", $order);
		if ($method == "stateReadAll" || $method == "stateCalledAll") {
			$limit = $o[0] . " " . $o[1];
		} else {
			$limit = $o[0] . " " . $o[1] . " LIMIT " . 
					(($position * $itensPerPage) - $itensPerPage) . ", " . $itensPerPage;
		}		
	} else {
		if ($method == "stateReadAll" || $method == "stateCalledAll") {
			$limit = "tipos_noticias.ordem ASC";	
		} else {
			if ($position > 0 && $itensPerPage > 0) {
				$limit = "tipos_noticias.id DESC LIMIT " . 
						(($position * $itensPerPage) - $itensPerPage) . ", " . $itensPerPage;	
			}
		}
	}
	
	/**************************************************
	 * Webpage
	 **************************************************/		
	
	/*
	 * Page
	 */
	if ($method == "page") {
		/*
		 * SEO
		 */
		$view->setTitle(ucfirst($screen));
		$view->setDescription("");
		$view->setKeywords("");
		
		$daoFactory->beginTransaction();
		$response["tipos_noticias"] = $daoFactory->getTipos_noticiasDao()->read($where, $limit, true);
		$daoFactory->close();
		
		if (isset($_GET["friendly"]))
			$view->setTitle($response["tipos_noticias"][0]["tipos_noticias.tipo_noticia"]);

		echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/header.html", $response);
		
		echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . 
				(isset($_GET["friendly"]) ? "/html/tipos_noticias.html" : "/html/tipos_noticias.html"), $response);
		
		echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/footer.html", $response);
	}
	
	/**************************************************
	 * Webservice
	 **************************************************/	

	/*
	 * Create
	 */
	else if ($method == "api-create") {
		enableCORS();
		if (isset($_POST["request"])) {
			$request = json_decode($_POST["request"], true);
			// $request[0]["@_PARAM"] = $daoFactory->prepare($request[0]["@_PARAM"]); // Prepare with sql injection.

			$daoFactory->beginTransaction();
			$tipos_noticias = new model\Tipos_noticias();
			$tipos_noticias->setTipo_noticia(logicNull($request["tipos_noticias.tipo_noticia"]));
			$tipos_noticias->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$tipos_noticias->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$tipos_noticias->setCor($request["tipos_noticias.cor"]);
			$tipos_noticias->setSituacao_registro($request["tipos_noticias.situacao_registro"]);
			
			$resultDao = $daoFactory->getTipos_noticiasDao()->create($tipos_noticias);

			if ($resultDao) {
				$daoFactory->commit();
				$response["message"] = "success";
			} else {							
				$daoFactory->rollback();
				$response["message"] = "error";
			}

			$daoFactory->close();
		} else {
			$response["message"] = "error";
		}
		
		echo $view->json($response);
	}
	
	/*
	 * Read
	 */
	else if ($method == "api-read") {
		enableCORS();
		
		if (isset($_POST["request"])) {
			$request = json_decode($_POST["request"], true);
			
			$limit = "tipos_noticias.id DESC LIMIT " . 
					(($request[0]["page"] * $request[0]["pageSize"]) - 
					$request[0]["pageSize"]) . ", " . $request[0]["pageSize"];	
		}
		
		$daoFactory->beginTransaction();
		$tipos_noticias = $daoFactory->getTipos_noticiasDao()->read("", $limit, false);
		$daoFactory->close();
		
		echo $view->json($tipos_noticias);
	}
	
	/*
	 * Update
	 */
	else if ($method == "api-update") {	
		enableCORS();
		if (isset($_POST["request"])) {
			$request = json_decode($_POST["request"], true);
			// $request[0]["@_PARAM"] = $daoFactory->prepare($request[0]["@_PARAM"]); // Prepare with sql injection.
			
			$tipos_noticias = new model\Tipos_noticias();
			$tipos_noticias->setId($request["tipos_noticias.id"]);
			$tipos_noticias->setTipo_noticia(logicNull($request["tipos_noticias.tipo_noticia"]));
			$tipos_noticias->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$tipos_noticias->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$tipos_noticias->setCor($request["tipos_noticias.cor"]);
			$tipos_noticias->setSituacao_registro($request["tipos_noticias.situacao_registro"]);
			
			$daoFactory->beginTransaction();
			$resultDao = $daoFactory->getTipos_noticiasDao()->update($tipos_noticias);

			if ($resultDao) {
				$daoFactory->commit();
				$response["message"] = "success";
			} else {							
				$daoFactory->rollback();
				$response["message"] = "error";
			}

			$daoFactory->close();
		} else {
			$response["message"] = "error";
		}
		
		echo $view->json($response);
	}
	
	/* 
	 * Delete
	 */
	else if ($method == "api-delete") {
		enableCORS();
		if (isset($_POST["request"])) {
			$request = json_decode($_POST["request"], true);
			$request["tipos_noticias.id"] = $daoFactory->prepare($request["tipos_noticias.id"]); // Prepare with sql injection.
				
			$result = true;
			$lines = explode("<gz>", $request["tipos_noticias.id"]);

			$daoFactory->beginTransaction();

			for ($i = 0; $i < sizeof($lines); $i++) {
				$where = "tipos_noticias.id = " . $lines[$i];
				
				$resultDao = $daoFactory->getTipos_noticiasDao()->delete($where);
				$result = !$result ? false : (!$resultDao ? false : true);
			}

			if ($result) {
				$daoFactory->commit();
				$response["message"] = "success";
			} else {							
				$daoFactory->rollback();
				$response["message"] = "error";
			}

			$daoFactory->close();
		} else {
			$response["message"] = "error";
		} 

		echo $view->json($response);
	}
	
	else if ($method == "changeOrder") {		
		$result = true;
		$daoFactory->beginTransaction();
		$call = $daoFactory->getTipos_noticiasDao()->read("tipos_noticias.id = " . $form[0], "", false);
		$answer = $daoFactory->getTipos_noticiasDao()->read("tipos_noticias.id = " . $form[1], "", false);
		$tipos_noticiasDao = $daoFactory->getTipos_noticiasDao()->read("tipos_noticias.ordem >= " . $answer[0]["tipos_noticias.ordem"], "", false);
		if (is_array($tipos_noticiasDao) && sizeof($tipos_noticiasDao) > 0) {
			for ($x = 0; $x < sizeof($tipos_noticiasDao); $x++) {
				$tipos_noticias = new model\Tipos_noticias();
				$tipos_noticias->setId($tipos_noticiasDao[$x]["tipos_noticias.id"]);
				$tipos_noticias->setTipo_noticia(logicNull($tipos_noticiasDao[$x]["tipos_noticias.tipo_noticia"]));
			$tipos_noticias->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$tipos_noticias->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$tipos_noticias->setCor($tipos_noticiasDao[$x]["tipos_noticias.cor"]);
			$tipos_noticias->setSituacao_registro($tipos_noticiasDao[$x]["tipos_noticias.situacao_registro"]);
			
				$resultDao = $daoFactory->getTipos_noticiasDao()->update($tipos_noticias);			
				$result = !$result ? false : (!$resultDao ? false : true);
			}
			$tipos_noticias = new model\Tipos_noticias();
			$tipos_noticias->setId($call[0]["tipos_noticias.id"]);
			$tipos_noticias->setTipo_noticia(logicNull($call[0]["tipos_noticias.tipo_noticia"]));
			$tipos_noticias->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$tipos_noticias->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$tipos_noticias->setCor($call[0]["tipos_noticias.cor"]);
			$tipos_noticias->setSituacao_registro($call[0]["tipos_noticias.situacao_registro"]);
			
			$resultDao = $daoFactory->getTipos_noticiasDao()->update($tipos_noticias);			
			$result = !$result ? false : (!$resultDao ? false : true);
		}
		if ($result) {
			$daoFactory->commit();
			$response[0]["message"] = "success";
		} else {							
			$daoFactory->rollback();
			$response[0]["message"] = "error";
		}
		$daoFactory->close();
		echo $darth->json($response);
	}
	
	/**************************************************
	 * System
	 **************************************************/	
	
	else {
		if (!getActiveSession($_ROOT . $_MODULE)) 
			echo "<script>goTo(\"/login/1\");</script>";
		else {
			/*
			 * Create
			 */
			if ($method == "stateCreate") {
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method))
					echo "<script>goTo(\"/login/1\");</script>";	
				else {
					$daoFactory->beginTransaction();
					$response["titles"] = $daoFactory->getTelasDao()->read("telas.identificador = \"" . $screen . "\"", "", true);
					$response["cores"] = $daoFactory->getCoresDao()->read("cores.tipo_cor = 1", "cores.id ASC", false);
					$response["situacoes_registros"] = $daoFactory->getSituacoes_registrosDao()->read("", "situacoes_registros.id ASC", false);
					$daoFactory->close();

					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/menus/menusCST.html", getMenu($daoFactory, $_USER, $screen));
					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/tipos_noticias/tipos_noticiasCRT.html", $response);
				}
			}

			/*
			 * Read
			 */
			else if ($method == "stateRead" || $method == "stateReadAll") {
				if ($method == "stateReadAll") {
					$method = "stateRead";
				}
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method))
					echo "<script>goTo(\"/login/1\");</script>";	
				else {
					$daoFactory->beginTransaction();
					$response["titles"] = $daoFactory->getTelasDao()->read("telas.identificador = \"" . $screen . "\"", "", true);
					$response["tipos_noticias"] = $daoFactory->getTipos_noticiasDao()->read($where, $limit, true);
					if (!is_array($response["tipos_noticias"])) {
						$response["data_not_found"][0]["value"] = "<p>Não possui registro.</p>";
					}
					$daoFactory->close();

					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/menus/menusCST.html", getMenu($daoFactory, $_USER, $screen));
					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/tipos_noticias/tipos_noticiasRD.html", $response);
				}
			}

			/*
			 * Update
			 */
			else if ($method == "stateUpdate") {
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method))
					echo "<script>goTo(\"/login/1\");</script>";	
				else {
					$daoFactory->beginTransaction();
					$response["titles"] = $daoFactory->getTelasDao()->read("telas.identificador = \"" . $screen . "\"", "", true);
					$response["tipos_noticias"] = $daoFactory->getTipos_noticiasDao()->read($where, "", true);
					$response["tipos_noticias"][0]["tipos_noticias.cores"] = $daoFactory->getCoresDao()->read("cores.tipo_cor = 1", "cores.id ASC", false);
					for ($x = 0; $x < sizeof($response["tipos_noticias"][0]["tipos_noticias.cores"]); $x++) {
						if ($response["tipos_noticias"][0]["tipos_noticias.cores"][$x]["cores.id"] == 
								$response["tipos_noticias"][0]["tipos_noticias.cor"]) {
							$response["tipos_noticias"][0]["tipos_noticias.cores"][$x]["cores.selected"] = "selected";
						}
					}
					$response["tipos_noticias"][0]["tipos_noticias.situacoes_registros"] = $daoFactory->getSituacoes_registrosDao()->read("", "situacoes_registros.id ASC", false);
					for ($x = 0; $x < sizeof($response["tipos_noticias"][0]["tipos_noticias.situacoes_registros"]); $x++) {
						if ($response["tipos_noticias"][0]["tipos_noticias.situacoes_registros"][$x]["situacoes_registros.id"] == 
								$response["tipos_noticias"][0]["tipos_noticias.situacao_registro"]) {
							$response["tipos_noticias"][0]["tipos_noticias.situacoes_registros"][$x]["situacoes_registros.selected"] = "selected";
						}
					}
					$daoFactory->close();

					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/menus/menusCST.html", getMenu($daoFactory, $_USER, $screen));
					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/tipos_noticias/tipos_noticiasUPD.html", $response);
				}
			}

			/*
			 * Called
			 */
			else if ($method == "stateCalled" || $method == "stateCalledAll") {
				if ($method == "stateCalledAll") {
					$method = "stateCalled";
				}
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method))
					echo "<script>goTo(\"/login/1\");</script>";	
				else {
					/*
					 * Insert your foreign key here
					 */
					if ($where != "")
						$where .= " AND tipos_noticias.@_FOREIGN_KEY = " . $base;
					else 
						$where = "tipos_noticias.@_FOREIGN_KEY = " . $base;
						
					$daoFactory->beginTransaction();
					$response["titles"] = $daoFactory->getTelasDao()->read("telas.identificador = \"" . $screen . "\"", "", true);
					$response["tipos_noticias"] = $daoFactory->getTipos_noticiasDao()->read($where, $limit, true);
					if (!is_array($response["tipos_noticias"])) {
						$response["data_not_found"][0]["value"] = "<p>Não possui registro.</p>";
					}
					$daoFactory->close();

					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/menus/menusCST.html", getMenu($daoFactory, $_USER, $screen));
					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/tipos_noticias/tipos_noticiasCLL.html", $response);
				}
			}

			/*
			 * Screen
			 */
			else if ($method == "screen") {
				if ($base != "") {
					$arrBase = explode("<gz>", $base);
					
					if (sizeof($arrBase) > 1) {
						if ($where != "")
							$where .= " AND tipos_noticias.@_FOREIGN_KEY = " . $arrBase[1];
						else
							$where = "tipos_noticias.@_FOREIGN_KEY = " . $arrBase[1];
					}
				}
				
				$limit = "tipos_noticias.id DESC LIMIT " . (($position * 5) - 5) . ", 5";

				$daoFactory->beginTransaction();
				$response["titles"] = $daoFactory->getTelasDao()->read("telas.identificador = \"" . $screen . "\"", "", true);
				$response["tipos_noticias"] = $daoFactory->getTipos_noticiasDao()->read($where, $limit, true);
				$daoFactory->close();

				echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/tipos_noticias/tipos_noticiasSCR.html", $response) . 
						"<size>" . (is_array($response["tipos_noticias"]) ? $response["tipos_noticias"][0]["tipos_noticias.size"] : 0) . "<theme>455a64";
			}

			/*
			 * Screen handler
			 */
			else if ($method == "screenHandler") {	
				$where = "";

				// Get value from combo
				$cmb = explode("<gz>", $search);

				if ($cmb[1] != "")
					$where = "tipos_noticias.id = " . $cmb[1];

				$daoFactory->beginTransaction();
				$response["tipos_noticias"] = $daoFactory->getTipos_noticiasDao()->comboScr($where);
				$daoFactory->close();

				echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/tipos_noticias/tipos_noticiasCMB.html", $response);
			}

			/*
			 * Create
			 */
			else if ($method == "create") {
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method)) {
					$response["message"] = "permission";
					
					echo $view->json($response);
				} else {
					$tipos_noticias = new model\Tipos_noticias();
					$tipos_noticias->setTipo_noticia(logicNull($form[0]));
					$tipos_noticias->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
					$tipos_noticias->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
					$tipos_noticias->setCor($form[1]);
					$tipos_noticias->setSituacao_registro($form[2]);
					
					$daoFactory->beginTransaction();
					$resultDao = $daoFactory->getTipos_noticiasDao()->create($tipos_noticias);

					if ($resultDao) {
						$daoFactory->commit();
						$response["message"] = "success";				
					} else {							
						$daoFactory->rollback();
						$response["message"] = "error";
					}

					$daoFactory->close();

					echo $view->json($response);
				}
			}

			/*
			 * Action update
			 */
			else if ($method == "update") {	
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method)) {
					$response["message"] = "permission";
					
					echo $view->json($response);
				} else {
					$tipos_noticias = new model\Tipos_noticias();
					$tipos_noticias->setId($code);
					$tipos_noticias->setTipo_noticia(logicNull($form[0]));
					$tipos_noticias->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
					$tipos_noticias->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
					$tipos_noticias->setCor($form[1]);
					$tipos_noticias->setSituacao_registro($form[2]);
					
					$daoFactory->beginTransaction();
					$resultDao = $daoFactory->getTipos_noticiasDao()->update($tipos_noticias);

					if ($resultDao) {
						$daoFactory->commit();
						$response["message"] = "success";
					} else {							
						$daoFactory->rollback();
						$response["message"] = "error";
					}

					$daoFactory->close();

					echo $view->json($response);
				}
			}
			
			/* 
			 * Action delete
			 */
			else if ($method == "delete") {
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method)) {
					$response["message"] = "permission";
					
					echo $view->json($response);
				} else {
					$result = true;
					$lines = explode("<gz>", $code);

					$daoFactory->beginTransaction();

					for ($i = 1; $i < sizeof($lines); $i++) {
						$where = "tipos_noticias.id = " . $lines[$i];
						
						$resultDao = $daoFactory->getTipos_noticiasDao()->delete($where);
						$result = !$result ? false : (!$resultDao ? false : true);
					}

					if ($result) {
						$daoFactory->commit();
						$response["message"] = "success";
					} else {							
						$daoFactory->rollback();
						$response["message"] = "error";
					}

					$daoFactory->close();

					echo $view->json($response);	
				}
			}
		}
	}

?>